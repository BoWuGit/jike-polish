{
  "name": "jike-polish",
  "private": true,
  "type": "module",
  "version": "1.1.1",
  "description": "即刻 Web 浏览器扩展（内容脚本构建与校验）",
  "scripts": {
    "build": "esbuild src/content.js --outfile=content.js --platform=browser --target=chrome109",
    "lint": "eslint src",
    "lint:fix": "eslint src --fix",
    "check": "npm run lint && npm run build"
  },
  "devDependencies": {
    "@eslint/js": "^9.21.0",
    "esbuild": "^0.25.0",
    "eslint": "^9.21.0",
    "globals": "^15.15.0"
  }
}
